{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-local",
  "description": "Task status and pet controls belong only to the bundled pet page",
  "windows": ["pet"],
  "local": true,
  "permissions": ["allow-pet-controls"]
}
